# Функция для приветствия (аналог void)
def greet():
    """Просто печатает приветствие, ничего не возвращает."""
    greetings = "Привет!"
    name = "Иржик"
    print(f"{greetings} {name}")

# Функция для вопроса о возрасте (аналог void)
def ask_and_recommend():
    """Спрашивает возраст и дает рекомендацию, ничего не возвращает."""
    age = int(input(f"Иржик, сколько тебе лет?\n"))

    if age == 5:
        print(f"Понятно, тебе {age} лет. Почитай Каштанку")
    elif age >= 6 and age <= 21:
        print(f"Понятно, тебе {age} лет. Почитай Курочку Рябу")
    else:
        print(f"Понятно, тебе {age} лет. Почитай Войну и Мир")

def recommend_book(name, age):
    """
    Рекомендует книгу на основе возраста.
    Параметры:
        name (str): Имя человека
        age (int): Возраст человека
    """
    print(f"\n{name}, тебе {age} лет.")

    if age == 5:
        print(f"Тебе стоит почитать 'Каштанку'.")
    elif 6 <= age <= 21:  # Более короткая запись условия
        print(f"Тебе стоит почитать 'Курочку Рябу'.")
    else:
        print(f"Тебе стоит почитать 'Войну и Мир'.")
def get_recommendation(age):
    """
    Возвращает название книги на основе возраста.
    Параметры:
        age (int): Возраст человека
    Возвращает:
        str: Название книги
    """
    if age == 5:
        return "Каштанку"
    elif 6 <= age <= 21:
        return "Курочку Рябу"
    else:
        return "Войну и Мир"

def get_recommendation(age: int) -> str:
    """
    Возвращает название книги на основе возраста.
    Параметры:
        age (int): Возраст человека
    Возвращает:
        str: Название книги
    """
    if age <= 5:
        return "Каштанку"
    elif 6 <= age <= 21:
        return "Курочку Рябу"
    else:
        return "Войну и Мир"

# Основная программа
def main():
    name = "Иржик"
    print(f"Привет, {name}!")

    user_age = int(input(f"{name}, сколько тебе лет?\n"))

    # Получаем результат работы функции
    book = get_recommendation(user_age)

    # Используем полученный результат
    print(f"Понятно, тебе {user_age} лет. Почитай {book}")

# Запуск
main()